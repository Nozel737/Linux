call variables: echo "$PORT", echo $App
create variables:
            App_name="my_app"
            PORT=8080
            ENV="production"
    After creating variables you can call them using "$" sign including the name before the variable i.e PORT=8080. Meaniing that to be able to get the value 8080, you should call the variable PORT.

To get or filter only error messages: grep "ERROR" filename.extention

Number of errors or fatal messages are: grep -c "ERROR" file name.extension